namespace Techie.Pbx.Asterisk.Provisioning
{
    /// <summary>
    /// The two kinds of image a phone background may be (D145): what every Poly phone accepts, and
    /// nothing a browser would have to convert first.
    /// </summary>
    public enum BackgroundImageFormat
    {
        Png,
        Jpeg,
    }
}
