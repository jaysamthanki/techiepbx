@model List<SettingSection>
@*
    Moved to Pages/Shared/_Sections.cshtml, because the Parking page uses it too and is not one of
    the settings pages (D119). This forwards rather than duplicating, so the markup lives in one
    place; it exists only so that the views beside it keep finding "_Sections" by name, and can be
    deleted once they are pointed at the shared one.
*@
<partial name="/Pages/Shared/_Sections.cshtml" model="Model" />
