@*
    Empty. The per-page "changes not applied" banner was replaced by the apply button in the
    navbar (D43), which is the same information in one place instead of on every page. Nothing
    renders this any more and the file can be deleted.
*@
