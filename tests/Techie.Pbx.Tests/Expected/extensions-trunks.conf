; Generated by TNPBX. Do not edit: changes will be overwritten.

[internal]
; Echo test
exten => *43,1,Answer()
 same => n,Playback(demo-echotest)
 same => n,Echo()
 same => n,Hangup()

[from-trunk-callcentric]
; Inbound calls from the callcentric trunk. No inbound routes yet.
exten => _X.,1,NoOp(Inbound call on trunk callcentric for ${EXTEN})
 same => n,Hangup()

[from-trunk-ip-provider]
; Inbound calls from the ip-provider trunk. No inbound routes yet.
exten => _X.,1,NoOp(Inbound call on trunk ip-provider for ${EXTEN})
 same => n,Hangup()
