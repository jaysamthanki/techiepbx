; Generated by TNPBX. Do not edit: changes will be overwritten.

[modules]
autoload = no

; Core services everything else needs
load = res_pjproject.so
load = res_rtp_asterisk.so
load = res_timing_timerfd.so
load = res_security_log.so

; Sorcery backends: how PJSIP objects and registrations are stored
load = res_sorcery_config.so
load = res_sorcery_memory.so
load = res_sorcery_astdb.so

; PJSIP: the only channel driver we use
load = res_pjsip.so
load = res_pjsip_session.so
load = res_pjsip_pubsub.so
load = res_pjsip_header_funcs.so
load = func_cut.so
load = chan_pjsip.so
load = res_pjsip_authenticator_digest.so
load = res_pjsip_outbound_authenticator_digest.so
load = res_pjsip_endpoint_identifier_user.so
load = res_pjsip_endpoint_identifier_ip.so
load = res_pjsip_registrar.so
load = res_pjsip_outbound_registration.so
load = res_pjsip_sdp_rtp.so
load = res_pjsip_caller_id.so
load = res_pjsip_nat.so
load = res_pjsip_dtmf_info.so
load = res_pjsip_notify.so
load = res_pjsip_mwi.so
load = res_pjsip_mwi_body_generator.so

; Bridging two people together
load = bridge_simple.so
load = bridge_native_rtp.so

; Dialplan and the applications it calls
load = pbx_config.so
load = app_dial.so
load = app_playback.so
load = app_echo.so
load = app_voicemail.so
load = func_callerid.so
load = func_timeout.so

; Audio: the formats our prompts and messages are stored in
load = codec_alaw.so
load = codec_g722.so
load = codec_slin.so
load = codec_ulaw.so
load = codec_gsm.so
load = format_gsm.so
load = format_pcm.so
load = format_wav.so
load = format_wav_gsm.so
