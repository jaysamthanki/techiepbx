; Generated by TNPBX. Do not edit: changes will be overwritten.

[internal]
; Echo test
exten => *43,1,Answer()
 same => n,Playback(demo-echotest)
 same => n,Echo()
 same => n,Hangup()

; Calls to the outside world, tried in the order the routes are listed
include => outbound

[from-trunk-callcentric]
; Inbound calls from the callcentric trunk. No inbound routes yet.
exten => _X.,1,NoOp(Inbound call on trunk callcentric for ${EXTEN})
 same => n,Hangup()

[outbound]
include => outbound-local
include => outbound-long-distance
include => outbound-blocked

[outbound-local]
; local (10) out over callcentric
exten => _NXXXXXXX,1,Dial(PJSIP/callcentric/${EXTEN},60)
 same => n,Hangup()

[outbound-long-distance]
; long-distance (20) out over callcentric
exten => _1NXXXXXXXXX,1,Dial(PJSIP/callcentric/${EXTEN},60)
 same => n,Hangup()

[outbound-blocked]
; No route matched, so the call does not go out. Toll fraud starts with a
; number nobody meant to allow, so unmatched numbers fail here on purpose.
exten => _X.,1,NoOp(No outbound route for ${EXTEN})
 same => n,Playback(ss-noservice)
 same => n,Hangup()
