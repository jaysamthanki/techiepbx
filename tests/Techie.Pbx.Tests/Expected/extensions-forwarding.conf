; Generated by TNPBX. Do not edit: changes will be overwritten.

[internal]
; Echo test
exten => *43,1,Answer()
 same => n,Playback(demo-echotest)
 same => n,Echo()
 same => n,Hangup()

; Check your own voicemail
exten => *97,1,Answer()
 same => n,VoiceMailMain(${CALLERID(num)}@default,s)
 same => n,Hangup()

; Directed call pickup: the code above followed by the extension that is ringing
exten => _*8.,1,Pickup(${EXTEN:2}@internal)
 same => n,Hangup()

; Front Desk
; Forwarding rings 1002 7146085242 instead of this extension's own phone (D130)
exten => 1001,hint,PJSIP/1001
exten => 1001,1,Dial(PJSIP/1002&Local/7146085242@internal/n,30,tTkKr)
 same => n,Hangup()

; O'Brien (Sales)
exten => 1002,hint,PJSIP/1002
exten => 1002,1,Dial(PJSIP/1002,30,tTkKr)
 same => n,GotoIf($["${DIALSTATUS}" = "BUSY"]?busy:unavailable)
 same => n(busy),VoiceMail(1002@default,b)
 same => n,Hangup()
 same => n(unavailable),VoiceMail(1002@default,u)
 same => n,Hangup()

; Cell Only
; Forwarding rings 7146085242 instead of this extension's own phone (D130)
exten => 1003,hint,PJSIP/1003
exten => 1003,1,Dial(Local/7146085242@internal/n,30,tTkKr)
 same => n,GotoIf($["${DIALSTATUS}" = "BUSY"]?busy:unavailable)
 same => n(busy),VoiceMail(1003@default,b)
 same => n,Hangup()
 same => n(unavailable),VoiceMail(1003@default,u)
 same => n,Hangup()

; Two Phones
; Forwarding rings 1004 7146085242 instead of this extension's own phone (D130)
exten => 1004,hint,PJSIP/1004
exten => 1004,1,Dial(PJSIP/1004&Local/7146085242@internal/n,30,tTkKr)
 same => n,Hangup()
