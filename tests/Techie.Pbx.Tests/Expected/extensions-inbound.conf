; Generated by TNPBX. Do not edit: changes will be overwritten.

[internal]
; Echo test
exten => *43,1,Answer()
 same => n,Playback(demo-echotest)
 same => n,Echo()
 same => n,Hangup()

; Check your own voicemail
exten => *97,1,Answer()
 same => n,VoiceMailMain(${CALLERID(num)}@default,s)
 same => n,Hangup()

; Front Desk
exten => 1001,1,Dial(PJSIP/1001,30)
 same => n,Hangup()

; Sales
exten => 1002,1,Dial(PJSIP/1002,30)
 same => n,GotoIf($["${DIALSTATUS}" = "BUSY"]?busy:unavailable)
 same => n(busy),VoiceMail(1002@default,b)
 same => n,Hangup()
 same => n(unavailable),VoiceMail(1002@default,u)
 same => n,Hangup()

[from-trunk-callcentric]
; Inbound calls from the callcentric trunk
; Main line
exten => 17771234567,1,NoOp(Inbound 17771234567 on callcentric to Extension:1001)
 same => n,Goto(internal,1001,1)
; Sales after hours
exten => 17771234568,1,NoOp(Inbound 17771234568 on callcentric to Voicemail:1002)
 same => n,VoiceMail(1002@default,u)
 same => n,Hangup()
exten => _X.,1,NoOp(Inbound any other number on callcentric to Hangup)
 same => n,Hangup()

[from-trunk-ip-provider]
; Inbound calls from the ip-provider trunk
exten => _X.,1,NoOp(No inbound route for ${EXTEN} on ip-provider)
 same => n,Hangup()
