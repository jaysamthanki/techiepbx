; Generated by TNPBX. Do not edit: changes will be overwritten.

[internal]
; Echo test
exten => *43,1,Answer()
 same => n,Playback(demo-echotest)
 same => n,Echo()
 same => n,Hangup()

; Check your own voicemail
exten => *97,1,Answer()
 same => n,VoiceMailMain(${CALLERID(num)}@default,s)
 same => n,Hangup()

; Front Desk
exten => 1001,1,Dial(PJSIP/1001,30)
 same => n,Hangup()

; Sales
exten => 1002,1,Dial(PJSIP/1002,30)
 same => n,GotoIf($["${DIALSTATUS}" = "BUSY"]?busy:unavailable)
 same => n(busy),VoiceMail(1002@default,b)
 same => n,Hangup()
 same => n(unavailable),VoiceMail(1002@default,u)
 same => n,Hangup()

; Support (All, 20s)
exten => 600,1,NoOp(Ring group 600 Support)
 same => n,Set(CALLERID(name)=Support: ${CALLERID(name)})
 same => n,Dial(PJSIP/1001&PJSIP/1002,20)
 same => n,VoiceMail(1002@default,u)
 same => n,Hangup()

; Sales team (Hunt, 15s)
exten => 601,1,NoOp(Ring group 601 Sales team)
 same => n,Dial(PJSIP/1002,15)
 same => n,Dial(PJSIP/1001,15)
 same => n,Goto(internal,600,1)
